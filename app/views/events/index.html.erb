<header class="masthead text-white text-center">
  <div class="column">
    <h2 class="mb-5">Hello there!</br></br>You wanna organize fake events for no reason, and still get a confirmation email anyway 😁?</h2>
    <%= link_to "Create your event here!", new_event_path, class: "btn btn-lg btn-primary" %>
  </div>
</header>

<div class="text-center my-5">
  <h4> ⬇️ Here are the latest Events ⬇️ </h4>
</div>

<div class="row justify-content-center text-center mb-5">
  <% @events.reverse.each do |event| %>

    <div class="card gossip-card m-3" style="width: 15rem">
      <div class="card-body">
        <h5 class="card-title"><%= event.title%></h5>
        <p class="card-text"><%= event.description%></p>
        <p class="card-text"><%= event.start_date%></p>
        <%= link_to "See this event", event_path(event.id), class: "btn btn-sm btn-primary" %>
      </div>
    </div>
    
  <% end %>
</div>
